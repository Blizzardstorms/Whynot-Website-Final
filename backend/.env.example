# PostgreSQL connection string
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DATABASE

# SMTP configuration for contact form (Nodemailer)
# Ensure these are set in production (Render) environment settings
SMTP_HOST=smtp.yourhost.co.za
SMTP_PORT=587
SMTP_USER=postmaster@ynotbuild.co.za
SMTP_PASS=your-password
TO_EMAIL=stafford@ynotbuild.co.za
